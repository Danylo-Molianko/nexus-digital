name: Автоматичне Розгортання Nexus Digital

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
    - name: 1. Отримання коду
      uses: actions/checkout@v4

    - name: 2. Налаштування SSH ключа
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: 3. Додавання сервера до known_hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan -H 164.90.234.176 >> ~/.ssh/known_hosts
        echo "✅ SSH налаштовано"

    - name: 4. Тестування SSH підключення
      run: |
        echo "🔍 Тестування SSH..."
        if ssh -o ConnectTimeout=10 nexususer@164.90.234.176 "echo 'SSH OK'"; then
          echo "✅ SSH підключення працює"
        else
          echo "❌ SSH підключення не вдалося"
          exit 1
        fi

    - name: 5. Розгортання на сервер
      run: |
        echo "🚀 Початок розгортання..."
        ssh -o ConnectTimeout=30 nexususer@164.90.234.176 << 'ENDSSH'
          # Функція для логування з кольорами
          log_info() { echo -e "\033[32m[INFO]\033[0m $1"; }
          log_error() { echo -e "\033[31m[ERROR]\033[0m $1"; }
          log_warn() { echo -e "\033[33m[WARN]\033[0m $1"; }
          
          # Зупинка при першій помилці
          set -e
          
          log_info "Перевірка системи..."
          PROJECT_DIR="/home/nexususer/nexus-digital-web"
          
          # Перевірка та створення директорії
          if [ ! -d "$PROJECT_DIR" ]; then
            log_info "Створення директорії проєкту..."
            mkdir -p "$PROJECT_DIR"
            cd "$PROJECT_DIR"
            
            # Ініціалізація git репозиторію
            if ! git clone https://github.com/Danylo-Molianko/nexus-digital.git . 2>/dev/null; then
              log_warn "Клонування не вдалося, ініціалізуємо порожній репозиторій..."
              git init
              git remote add origin https://github.com/Danylo-Molianko/nexus-digital.git
            fi
          else
            log_info "Оновлення існуючого проєкту..."
            cd "$PROJECT_DIR"
            
            # Збереження локальних змін
            if [ -n "$(git status --porcelain 2>/dev/null)" ]; then
              log_info "Збереження локальних змін..."
              git stash || log_warn "Не вдалося зберегти зміни"
            fi
            
            # Оновлення з GitHub
            log_info "Завантаження останніх змін..."
            git fetch origin main || log_warn "Fetch не вдався"
            git reset --hard origin/main || git reset --hard HEAD
          fi
          
          # Створення package.json якщо не існує
          if [ ! -f "package.json" ]; then
            log_info "Створення package.json..."
            cat > package.json << 'PACKAGE_JSON'
        {
          "name": "nexus-digital",
          "version": "1.0.0",
          "description": "Nexus Digital Website",
          "main": "server.js",
          "scripts": {
            "start": "node server.js",
            "production": "pm2 restart nexus-digital || pm2 start server.js --name nexus-digital",
            "stop": "pm2 stop nexus-digital || true"
          },
          "dependencies": {
            "express": "^4.18.2"
          }
        }
        PACKAGE_JSON
          fi
          
          # Створення server.js якщо не існує
          if [ ! -f "server.js" ]; then
            log_info "Створення server.js..."
            cat > server.js << 'SERVER_JS'
        const express = require('express');
        const path = require('path');
        const app = express();
        const PORT = process.env.PORT || 8080;
        
        console.log('🚀 Запуск Nexus Digital...');
        
        // Статичні файли
        app.use(express.static(path.join(__dirname, 'public')));
        app.use('/assets', express.static(path.join(__dirname, 'assets')));
        
        // Основний маршрут
        app.get('/', (req, res) => {
            const indexPath = path.join(__dirname, 'public', 'index.html');
            res.sendFile(indexPath, (err) => {
                if (err) {
                    console.error('Помилка відправки файлу:', err);
                    res.status(500).send('Помилка сервера');
                }
            });
        });
        
        // Обробка всіх інших маршрутів
        app.get('*', (req, res) => {
            const indexPath = path.join(__dirname, 'public', 'index.html');
            res.sendFile(indexPath, (err) => {
                if (err) {
                    res.status(404).send('Сторінка не знайдена');
                }
            });
        });
        
        // Запуск сервера
        app.listen(PORT, '0.0.0.0', () => {
            console.log(`✅ Сервер запущено на порту ${PORT}`);
            console.log(`🌐 Доступно: http://localhost:${PORT}`);
        });
        SERVER_JS
          fi
          
          # Встановлення Node.js якщо не встановлено
          if ! command -v node >/dev/null 2>&1; then
            log_warn "Node.js не знайдено, встановлюємо..."
            curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
            sudo apt-get install -y nodejs
          fi
          
          # Встановлення залежностей
          log_info "Встановлення залежностей..."
          if command -v npm >/dev/null 2>&1; then
            npm install --production --no-audit --no-fund
          else
            log_error "npm не знайдено"
            exit 1
          fi
          
          # Встановлення PM2 якщо не встановлено
          if ! command -v pm2 >/dev/null 2>&1; then
            log_info "Встановлення PM2..."
            sudo npm install -g pm2
          fi
          
          # Запуск/перезапуск сервера
          log_info "Запуск сервера..."
          if pm2 describe nexus-digital >/dev/null 2>&1; then
            log_info "Перезапуск існуючого додатку..."
            pm2 restart nexus-digital
          else
            log_info "Запуск нового додатку..."
            pm2 start server.js --name nexus-digital
          fi
          
          # Збереження конфігурації PM2
          pm2 save
          pm2 startup | tail -1 | sudo sh || log_warn "PM2 startup не налаштовано"
          
          # Перезапуск Nginx якщо встановлено
          if command -v nginx >/dev/null 2>&1; then
            log_info "Перезапуск Nginx..."
            sudo systemctl reload nginx || log_warn "Nginx reload не вдався"
          fi
          
          # Перевірка статусу
          log_info "Перевірка статусу додатку..."
          if pm2 describe nexus-digital | grep -q "online"; then
            log_info "✅ Додаток працює онлайн"
          else
            log_warn "⚠️ Додаток може працювати не стабільно"
          fi
          
          log_info "🎉 Розгортання завершено успішно!"
        ENDSSH

    - name: 6. Перевірка статусу сайту
      run: |
        echo "🔍 Перевірка доступності сайту..."
        sleep 10
        
        # Перевірка з тайм-аутом
        if timeout 30 bash -c 'until curl -f -s http://164.90.234.176 >/dev/null; do sleep 2; done'; then
          echo "✅ Сайт доступний за адресою: http://164.90.234.176"
        else
          echo "⚠️ Сайт поки недоступний, але розгортання завершено"
          echo "Перевірте логи сервера вручну"
        fi

    - name: 7. Показати статус PM2
      run: |
        echo "📊 Статус додатків на сервері:"
        ssh nexususer@164.90.234.176 "pm2 list || echo 'PM2 недоступний'"
      continue-on-error: true
